{
  "name": "web3-builders-dao",
  "private": true,
  "version": "0.1.0",
  "description": "Web3 Builders DAO - A decentralized autonomous organization for web3 builders",
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "turbo": "^2.5.4",
    "typescript": "5.8.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@11.3.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "cron": "^4.3.0",
    "date-fns": "^4.1.0",
    "next": "^15.3.3"
  }
}
